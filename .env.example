# RSS
FEED_URL=https://techcrunch.com/feed/

# AI provider: gemini or ollama
MODEL_PROVIDER=gemini

# If Gemini quota is hit, try local Ollama before skipping.
ENABLE_OLLAMA_FALLBACK=true

# If Gemini quota is hit and no fallback succeeds, exit cleanly (code 0).
SKIP_ON_GEMINI_QUOTA=true

# If selected Gemini model is not available on current API/version, exit cleanly (code 0).
SKIP_ON_GEMINI_MODEL_NOT_FOUND=true

# If LinkedIn UI times out in CI, skip this run instead of failing.
SKIP_ON_LINKEDIN_TIMEOUT=true

# Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite

# Ollama (optional local fallback)
OLLAMA_MODEL=llama3.1
OLLAMA_URL=http://localhost:11434/api/generate

# LinkedIn session state file generated by save_linkedin_session.py
LINKEDIN_STATE_FILE=linkedin_state.json

# Fallback login when session expires (optional but recommended)
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=

# Runtime
HEADLESS=true
LOG_LEVEL=INFO
